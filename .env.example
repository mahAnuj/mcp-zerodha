# Zerodha Kite Connect app credentials.
# Create an app at https://developers.kite.trade/ to get these.
#
# A .env file is optional: MCP clients can pass these through the `env` block
# of their server config instead.
API_KEY=your_kite_api_key
API_SECRET=your_kite_api_secret
